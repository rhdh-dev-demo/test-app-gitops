# Staging environment overrides
# This file extends Values.yaml with staging-specific configurations

app:
  environment: staging

# Image tag
image:
  tag: "staging-v1"
  digest: "sha256:750e404ff7676eaf88bcb3b77399757700bf48d4b8aac548c29cc8826722e55d"

# Environment variables - override for staging
env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: "8080"
  - name: APP_ENV
    value: "staging"

replicaCount: 2

resources:
  limits:
    cpu: 1000m
    memory: 1Gi
  requests:
    cpu: 200m
    memory: 256Mi

route:
  enabled: true
  host: ""  # Will be auto-generated or set via ArgoCD values override

