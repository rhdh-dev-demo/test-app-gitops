app:
  name: test-app
  environment: dev
  namespace: test-app-dev
  clusterDomain: apps.rosa.kup9t-xin7u-x37.ssaj.p3.openshiftapps.com

image:
  host: image-registry.openshift-image-registry.svc:5000
  organization: test-app-dev
  name: test-app
  # Initial tag - will be updated by pipeline with git SHA
  tag: "latest"
  digest: "sha256:4c662506c9249477abc481788d3bda726e1e64134f7bfdac589726fd1f17ab15"
  # Digest - populated by pipeline for OpenShift cross-namespace pulls
  # When set, deployment uses image@digest format instead of image:tag
  # digest: "sha256:..."
  pullPolicy: Always
  # Full image path: {{ .Values.image.host }}/{{ .Values.image.organization }}/{{ .Values.image.name }}:{{ .Values.image.tag }}

replicaCount: 1

service:
  type: ClusterIP
  port: 8080
  targetPort: 8080

route:
  enabled: true
  host: ""
  tls:
    enabled: true
    insecureEdgeTerminationPolicy: Redirect
    termination: edge

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 100m
    memory: 128Mi

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 80

livenessProbe:
  httpGet:
    path: /health
    port: 8080
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  httpGet:
    path: /ready
    port: 8080
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

env:
  - name: NODE_ENV
    value: "production"
  - name: PORT
    value: "8080"
  - name: APP_ENV
    value: "dev"

serviceAccount:
  create: true
  name: ""

configMap:
  enabled: false
  data: {}

